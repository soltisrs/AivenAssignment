--- This is the SQL command used for the creation of the sensor_readings table ---

CREATE TABLE sensor_readings (
    sensor_id TEXT,
    temperature FLOAT,
    humidity INT,
    timestamp TIMESTAMP
);
